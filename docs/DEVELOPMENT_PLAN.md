# План разработки модульной системы file2text

## 🎯 Цель
Превратить проект в универсальный Python-модуль для:
- Конвертации аудио/видео в текст
- Суммаризации текста
- Векторизации текста
- Использования в других проектах как библиотека

---

## 📋 Этап 1: Рефакторинг в модульную структуру

### 1.1 Создание структуры пакета
```
file2text/
├── __init__.py
├── core/
│   ├── __init__.py
│   ├── transcriber.py      # Транскрипция (Whisper)
│   ├── diarizer.py         # Диаризация спикеров
│   ├── summarizer.py       # Суммаризация
│   └── vectorizer.py       # Векторизация (новый модуль)
├── utils/
│   ├── __init__.py
│   ├── audio_converter.py  # Конвертация аудио (ffmpeg)
│   ├── text_cleaner.py     # Очистка текста от артефактов
│   └── config.py           # Конфигурация
├── cli/
│   ├── __init__.py
│   └── main.py             # CLI интерфейс
└── examples/
    ├── basic_usage.py
    ├── batch_processing.py
    └── api_integration.py
```

### 1.2 Разделение функциональности

**core/transcriber.py**
- Класс `Transcriber` с методами:
  - `transcribe(audio_path, **kwargs)` - транскрипция
  - `transcribe_batch(audio_paths)` - пакетная обработка
  - `get_segments()` - получение сегментов с временными метками

**core/diarizer.py**
- Класс `Diarizer` с методами:
  - `diarize(audio_path)` - диаризация
  - `assign_speakers(segments, diarization)` - сопоставление спикеров

**core/summarizer.py**
- Класс `Summarizer` с методами:
  - `summarize(text, **kwargs)` - суммаризация текста
  - `summarize_by_speakers(speaker_text)` - суммаризация по спикерам
  - `summarize_full(transcript)` - суммаризация всего разговора

**core/vectorizer.py** (новый)
- Класс `Vectorizer` с методами:
  - `vectorize(text)` - векторизация текста
  - `vectorize_batch(texts)` - пакетная векторизация
  - `similarity(text1, text2)` - вычисление схожести
  - `search(query, vectors, top_k=5)` - поиск по векторам

---

## 📋 Этап 2: Реализация векторизации

### 2.1 Выбор модели для векторизации
- **Вариант 1**: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2` (уже используется частично)
- **Вариант 2**: `intfloat/multilingual-e5-large` (лучшее качество)
- **Вариант 3**: `cointegrated/rubert-tiny2` (быстро, для русского)

### 2.2 Функциональность векторизатора
- Векторизация текста в embeddings
- Поиск похожих текстов (cosine similarity)
- Кластеризация текстов по векторам
- Сохранение/загрузка векторов (для кэширования)

---

## 📋 Этап 3: Создание единого API

### 3.1 Главный класс `File2Text`
```python
from file2text import File2Text

processor = File2Text(
    whisper_model="medium",
    device="cuda"
)

# Полный пайплайн
result = processor.process(
    audio_path="audio.mp3",
    transcribe=True,
    diarize=True,
    summarize=True,
    vectorize=True
)

# Доступ к результатам
print(result.text)              # Полный текст
print(result.speakers)          # Текст по спикерам
print(result.summary)           # Суммаризация
print(result.vectors)           # Векторы
```

### 3.2 Поэтапная обработка
```python
# Только транскрипция
text = processor.transcribe("audio.mp3")

# Транскрипция + диаризация
result = processor.transcribe_with_speakers("audio.mp3")

# Добавить суммаризацию
summary = processor.summarize(result.text)

# Добавить векторизацию
vectors = processor.vectorize(result.text)
```

---

## 📋 Этап 4: Конфигурация и настройки

### 4.1 Файл конфигурации `config.py`
```python
class Config:
    # Whisper
    WHISPER_MODEL = "medium"
    WHISPER_DEVICE = "cuda"
    
    # Диаризация
    HUGGINGFACE_TOKEN = os.getenv("HUGGINGFACE_TOKEN")
    
    # Суммаризация
    SUMMARIZER_MODEL = "IlyaGusev/rut5_base_sum_gazeta"
    SUMMARY_MAX_LENGTH = 250
    SUMMARY_MIN_LENGTH = 50
    
    # Векторизация
    VECTORIZER_MODEL = "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
    VECTOR_DIMENSION = 384
    
    # Пути
    DEFAULT_OUTPUT_DIR = "./output"
```

### 4.2 Поддержка .env файлов
- Использование `python-dotenv` для загрузки переменных окружения
- Пример `.env.example` в репозитории

---

## 📋 Этап 5: Установка как пакет

### 5.1 Создание `setup.py`
```python
from setuptools import setup, find_packages

setup(
    name="file2text",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "openai-whisper",
        "pyannote.audio",
        "transformers",
        "torch",
        "sentence-transformers",
        "python-dotenv",
    ],
    entry_points={
        "console_scripts": [
            "file2text=file2text.cli.main:main",
        ],
    },
)
```

### 5.2 Установка
```bash
pip install -e .
# или
pip install git+https://github.com/yourusername/file2text.git
```

---

## 📋 Этап 6: CLI интерфейс

### 6.1 Командная строка
```bash
# Базовое использование
file2text audio.mp3

# С опциями
file2text audio.mp3 --summarize --vectorize --output ./results

# Пакетная обработка
file2text --batch ./audio_folder --model large-v2

# Только векторизация существующего текста
file2text --vectorize-only text.txt
```

---

## 📋 Этап 7: Документация и примеры

### 7.1 README.md
- Описание проекта
- Установка
- Быстрый старт
- API документация
- Примеры использования

### 7.2 Примеры кода
- `examples/basic_usage.py` - базовое использование
- `examples/batch_processing.py` - пакетная обработка
- `examples/api_integration.py` - интеграция в другие проекты
- `examples/vector_search.py` - поиск по векторам

---

## 📋 Этап 8: Тестирование

### 8.1 Unit тесты
- Тесты для каждого модуля
- Моки для моделей (чтобы не загружать их в тестах)

### 8.2 Интеграционные тесты
- Тесты полного пайплайна
- Тесты с реальными аудио файлами

---

## 📋 Этап 9: Оптимизация и производительность

### 9.1 Кэширование
- Кэш векторов (чтобы не пересчитывать)
- Кэш транскрипций

### 9.2 Параллельная обработка
- Многопоточность для пакетной обработки
- Асинхронная обработка (опционально)

---

## 📋 Этап 10: Дополнительные возможности

### 10.1 Экспорт результатов
- JSON формат
- CSV для табличных данных
- SQLite база данных для хранения результатов

### 10.2 Интеграции
- Поддержка различных форматов аудио
- Интеграция с базами данных
- REST API (опционально, для веб-приложений)

---

## 🚀 Порядок реализации

### Фаза 1 (Неделя 1-2): Рефакторинг
1. ✅ Создать структуру пакета
2. ✅ Разделить код на модули
3. ✅ Создать базовые классы

### Фаза 2 (Неделя 3): Векторизация
1. ✅ Реализовать Vectorizer
2. ✅ Добавить поиск по векторам
3. ✅ Интегрировать в основной пайплайн

### Фаза 3 (Неделя 4): API и конфигурация
1. ✅ Создать единый API (File2Text класс)
2. ✅ Настроить конфигурацию
3. ✅ Добавить поддержку .env

### Фаза 4 (Неделя 5): Установка и CLI
1. ✅ Создать setup.py
2. ✅ Реализовать CLI
3. ✅ Протестировать установку

### Фаза 5 (Неделя 6): Документация и примеры
1. ✅ Написать README
2. ✅ Создать примеры
3. ✅ Добавить docstrings

---

## 📝 Примечания

- Сохранить обратную совместимость со старыми скриптами
- Миграция должна быть постепенной
- Старые файлы (main.py, summarize.py) можно оставить как примеры или обернуть в CLI

---

## 🔧 Технический стек

- **Транскрипция**: OpenAI Whisper
- **Диаризация**: pyannote.audio
- **Суммаризация**: transformers (RUT5)
- **Векторизация**: sentence-transformers
- **Аудио**: ffmpeg-python (опционально) или subprocess
- **Конфигурация**: python-dotenv

---

## ✅ Критерии готовности

- [ ] Проект можно установить через pip
- [ ] Есть единый API для всех функций
- [ ] Работает векторизация
- [ ] Есть CLI интерфейс
- [ ] Есть документация и примеры
- [ ] Можно использовать как модуль в других проектах
