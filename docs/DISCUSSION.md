# Обсуждение архитектуры file2text

## 🤔 Вопросы для обсуждения

### 1. Структура пакета и именование

**Текущее состояние:** Проект называется `file2text`

**Варианты:**
- ✅ **A) Оставить `file2text`** - простое, понятное имя
- B) Переименовать в `audio2text` - более точное описание
- C) Переименовать в `speech2text` - акцент на речь
- D) Оставить `file2text`, но добавить алиасы для импорта

**Рекомендация:** Оставить `file2text` - имя уже используется, менять не стоит.

---

### 2. Уровень модульности

**Варианты:**
- ✅ **A) Строгая модульность** - каждый класс в отдельном файле
  ```
  core/
    ├── transcriber.py
    ├── diarizer.py
    ├── summarizer.py
    └── vectorizer.py
  ```
  
- B) Группировка по функциональности
  ```
  core/
    ├── transcription.py  # Transcriber + Diarizer
    ├── summarization.py  # Summarizer
    └── vectorization.py  # Vectorizer
  ```

- C) Гибридный подход
  ```
  core/
    ├── transcription/
    │   ├── transcriber.py
    │   └── diarizer.py
    ├── summarization/
    │   └── summarizer.py
    └── vectorization/
        └── vectorizer.py
  ```

**Рекомендация:** Вариант A - проще навигация, легче тестирование.

---

### 3. Векторизация - уровень функциональности

**Варианты:**
- A) Базовый - только векторизация текста
  ```python
  vectorizer.vectorize(text)  # → numpy array
  ```

- ✅ **B) Расширенный** - поиск, кластеризация, сохранение
  ```python
  vectorizer.vectorize(text)
  vectorizer.similarity(text1, text2)
  vectorizer.search(query, texts, top_k=5)
  vectorizer.save_vectors(vectors, path)
  vectorizer.load_vectors(path)
  ```

- C) Полный - векторная БД, индексация
  ```python
  # + интеграция с FAISS, Qdrant, Chroma
  vectorizer.create_index(vectors)
  vectorizer.search_index(query, top_k=5)
  ```

**Рекомендация:** Вариант B - хороший баланс функциональности и сложности.

---

### 4. Обратная совместимость

**Текущие файлы:** `main.py`, `summarize.py`, `logicCombrain.py`

**Варианты:**
- A) Сохранить как есть - пользователи могут продолжать использовать
- ✅ **B) Обернуть в CLI команды** - `file2text transcribe`, `file2text summarize`
- C) Перенести в `examples/` - как примеры использования
- D) Удалить после миграции - чистая архитектура

**Рекомендация:** Вариант B - сохраняем функциональность, но через новый интерфейс.

---

### 5. Конфигурация

**Варианты:**
- A) Только переменные окружения + `.env`
  ```python
  # .env
  HUGGINGFACE_TOKEN=...
  WHISPER_MODEL=medium
  ```

- ✅ **B) Файл конфигурации + переменные окружения** (приоритет у env)
  ```python
  # config.yaml
  whisper:
    model: medium
    device: cuda
  
  # .env переопределяет config.yaml
  WHISPER_MODEL=large-v2
  ```

- C) Только Python конфиг класс
  ```python
  from file2text import Config
  config = Config(whisper_model="medium")
  ```

**Рекомендация:** Вариант B - гибкость + удобство.

---

### 6. Формат результатов

**Варианты:**
- ✅ **A) Python объекты (dataclass)** - основной формат
  ```python
  @dataclass
  class ProcessingResult:
      text: str
      speakers: Dict[str, str]
      summary: Dict[str, str]
      vectors: np.ndarray
  ```

- B) JSON файлы - для сохранения
- ✅ **C) Оба варианта** - объекты для работы, JSON для сохранения
  ```python
  result = processor.process("audio.mp3")
  result.to_json("result.json")  # опционально
  result.to_sqlite("results.db")  # опционально
  ```

**Рекомендация:** Вариант C - максимум гибкости.

---

### 7. CLI интерфейс

**Варианты:**
- A) Простой через `argparse`
  ```bash
  file2text audio.mp3 --summarize
  ```

- ✅ **B) Продвинутый через `typer`** - современный, с автодополнением
  ```bash
  file2text transcribe audio.mp3
  file2text summarize text.txt
  file2text vectorize text.txt
  file2text process audio.mp3 --all
  ```

- C) Минимальный - основной упор на API

**Рекомендация:** Вариант B - `typer` дает отличный UX.

---

### 8. Зависимости

**Варианты:**
- A) Минимальные - только необходимое
- B) Удобство использования - больше зависимостей
- ✅ **C) Опциональные зависимости (extras)**
  ```python
  # Минимальная установка
  pip install file2text
  
  # С дополнительными возможностями
  pip install file2text[vectorization]
  pip install file2text[cli]
  pip install file2text[all]
  ```

**Рекомендация:** Вариант C - гибкость для разных случаев использования.

---

### 9. Кэширование и производительность

**Вопросы:**
- Нужно ли кэширование транскрипций? (да/нет)
- Нужно ли кэширование векторов? (да/нет)
- Где хранить кэш? (папка `.cache/`, SQLite, память)

**Рекомендация:**
- ✅ Кэш векторов - да (пересчет дорогой)
- ✅ Кэш транскрипций - опционально (можно пересчитать)
- Хранить в `.cache/file2text/` или SQLite

---

### 10. Интеграция с существующим кодом

**Вопросы:**
- Что делать с `logicCombrain.py` (BERTopic кластеризация)?
- Нужна ли интеграция с векторными БД (FAISS, Qdrant, Chroma)?
- Нужен ли REST API для веб-приложений?

**Рекомендация:**
- `logicCombrain.py` → перенести в `core/topic_modeler.py`
- Векторные БД → опциональная интеграция через extras
- REST API → отдельный пакет или пример

---

## 📝 Предложения по реализации

### Приоритет 1 (MVP - минимально жизнеспособный продукт):
1. ✅ Рефакторинг в модули (core/, utils/)
2. ✅ Базовый класс File2Text
3. ✅ Векторизация (базовый уровень)
4. ✅ Простой CLI
5. ✅ setup.py для установки

### Приоритет 2 (Расширенная функциональность):
1. Расширенная векторизация (поиск, кластеризация)
2. Конфигурация через YAML + .env
3. Кэширование
4. Пакетная обработка

### Приоритет 3 (Дополнительные возможности):
1. Интеграция с векторными БД
2. REST API
3. Веб-интерфейс (опционально)
4. Расширенная документация

---

## 🎯 Рекомендуемый подход

**Структура:**
```
file2text/
├── __init__.py              # Главный экспорт
├── core/
│   ├── transcriber.py
│   ├── diarizer.py
│   ├── summarizer.py
│   ├── vectorizer.py
│   └── topic_modeler.py    # Из logicCombrain.py
├── utils/
│   ├── audio_converter.py
│   ├── text_cleaner.py
│   └── config.py
├── cli/
│   └── main.py
└── examples/
    └── ...
```

**API дизайн:**
```python
# Простой импорт
from file2text import File2Text

# Инициализация
processor = File2Text()

# Полный пайплайн
result = processor.process("audio.mp3", 
    transcribe=True,
    diarize=True,
    summarize=True,
    vectorize=True
)

# Поэтапная обработка
text = processor.transcribe("audio.mp3")
summary = processor.summarize(text)
vectors = processor.vectorize(text)
```

**CLI:**
```bash
# Установка
pip install file2text

# Использование
file2text process audio.mp3 --all
file2text transcribe audio.mp3
file2text summarize text.txt
file2text vectorize text.txt
```

---

## ❓ Вопросы для уточнения

1. **Какой уровень векторизации нужен?** (A/B/C)
2. **Нужно ли кэширование?** (да/нет)
3. **Какой формат результатов предпочтителен?** (объекты/JSON/оба)
4. **Нужна ли интеграция с векторными БД?** (да/нет)
5. **Какой CLI интерфейс предпочтителен?** (простой/продвинутый)
6. **Что делать со старыми скриптами?** (сохранить/обернуть/удалить)

---

## ✅ Следующие шаги

После обсуждения:
1. Уточнить архитектуру на основе ответов
2. Создать детальный план реализации
3. Начать с Фазы 1 - рефакторинг
